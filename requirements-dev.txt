# GMG Investment Intelligence — development and test dependencies
#
#     pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest==9.1.1
pytest-cov==7.1.0
playwright==1.62.0            # browser QA; run `playwright install chromium`
